# Spike:地表著色資料源 — 土地使用分區 vs OSM landuse vs 正射影像

> **狀態**:完成 — **交 owner / RFC 裁決,本報告不作成決定。**
> **建議**:主案 **B + A 混合**(OSM landuse 為主分類、都市計畫使用分區補洞,全部離線編譯);備案 **B 單獨**;**C(正射影像)不採用**。理由與代價見 **§8**。
> **Ticket**:FTP-69
> **性質**:工程盡職調查(engineering due diligence),**非正式法律意見**。
> **實測日期**:**2026-08-11**(UTC)。OSM 與線上服務皆為活資料,**本報告所有點值僅代表該日該次量測**。
> **產物**:本報告 + 可重跑量測 script [`ground-probe.py`](ground-probe.py)(實驗用,不進正式模組;自我測試 [`ground-probe.selftest.py`](ground-probe.selftest.py),23 案全綠)
> **關聯**:FTP-39(現況:地面為 Cesium 預設橢球灰)、FTP-40、FTP-33、FTP-35、FTP-29、[FTP-6 授權裁定](../../LICENSING.md)、[FTP-5 NLSC 實測](nlsc.md)

## 查證清單(exam;declaration commit 凍結)

每一項完成的標準:**附產生它的指令**與該指令的實際輸出。
未達標者標記 `unverified` 並寫明原因。**推論不得充當證據;文件描述不得代替量測。**

### AC1 — 三個候選各有實際下載/請求的證據

- [x] A(data.taipei 土地使用分區):實際下載,記錄格式、CRS、授權、更新日期、檔案大小 → **§4**
- [x] B(OSM landuse/natural/leisure):實際取得 M1 bbox 範圍資料,記錄取得方式與時間 → **§5**
- [x] C(正射影像):實際請求,記錄端點、是否需金鑰、授權條款出處 → **§6**
- [x] 每一項來源不可用時**據實記錄**,不以文件描述代替 → **§4.1**、**§9**

### AC2 — M1 bbox 內的覆蓋率與分類數(每個數字附指令)

- [x] M1 bbox 面積基準值,來自 `contracts/constants/m1_area.json`(不另抄一份)→ **§3**
- [x] A 的覆蓋率、分類數、各類面積 → **§4.3**
- [x] B 的覆蓋率、分類數、各類面積 → **§5.2**
- [x] **未被覆蓋的部分是什麼** → **§7**(拆成路廊/建物/真空白三塊)
- [x] **headline 覆蓋率以兩種獨立方法量測並對帳**(精確聯集 vs 網格取樣)→ **§2.2**
- [x] **任何回報「0」的量測附一個會回報非零的對照組** → **§2.3**

### AC3 — A 的粒度問題(決定 A 能不能用)

- [x] **明確回答:街廓內部有沒有細分?** → **§4.4**(**主計圖沒有、細計圖有**,附尺寸分佈)
- [x] 河岸、公園、空地在 A 裡分不分得出來 → **§4.5**
- [x] A 的幾何品質:自交、重疊、洞 → **§4.6**

### AC4 — B 的幾何品質與可重用性

- [x] polygon 閉合性、重疊、無效幾何計數 → **§5.3**
- [x] **能不能共用 FTP-29 既有的 bbox 裁切器** → **§5.4**(**不能**,附面積佔比)
- [x] 授權:**引用 FTP-6 的結論**,不重新論證 → **§10**

### AC5 — C 的執行期外部依賴

- [x] 可用性取樣 **≥20 次**,附時間戳與狀態分佈 → **§6.2**(48 次)
- [x] 能不能離線快取進 tile(授權是否允許)→ **§6.3**(**`unverified`**)
- [x] 若不可用/不採用,寫明放棄了什麼 → **§8.3**

### AC6 — 邊界(bbox 邊緣)

- [x] 跨 bbox 邊界的 polygon 被裁切後,邊緣會不會出現空白帶 → **§2.4**(**沒有**,附邊緣帶 vs 內部對比)
- [x] 完全在 bbox 外的 polygon 不貢獻覆蓋(對照組)→ **§2.3**

### AC7 — 裁決依據

- [x] 明確一個**主案** + 一個**備案** → **§8**
- [x] 寫出**選它會放棄什麼** → **§8.1**、**§8.2**、**§8.3**
- [x] 若建議 B:回答**要不要用 A 補洞**(混合方案)→ **§7.3**(**要**,附邊際效益數字)
- [x] 風格取捨(示意圖 vs 照片)已回答 → **§8.4**
- [x] 產出可供 RFC 引用,**不含任何實作** → 本 PR 只動 `docs/spikes/` 與 `docs/README.md`

### 紀律(全篇適用)

- [x] **不回述任何上游 response body** —— 只寫類別與計數(probe 以 allow-list 釘住,見 **§2.5**)
- [x] 所有點值數字**標日期**
- [x] 成因不明時寫「觀察到 X,不知道成因」,**不貼成因標籤**
- [x] 做不到的量測**大聲寫出來** → **§9**

## 1. 結論摘要(先看這張表)

M1 信義 bbox = 12,250,000 m²。所有百分比皆以此為分母。**量測日 2026-08-11。**

| | **A** 都市計畫使用分區(細計圖) | **B** OSM landuse/natural/leisure | **C** NLSC 正射影像 |
|---|---|---|---|
| 取得 | data.taipei SHP,4,187,541 bytes | Overpass,areas 2,939,943 bytes | WMTS 圖磚,單磚約 20–26 KB |
| 授權 | [OGDL v1](https://data.gov.tw/license) | [ODbL 1.0](https://opendatacommons.org/licenses/odbl/1-0/) | **`unverified`**(見 §10.3) |
| 需金鑰 | 否 | 否 | 否(48/48 無憑證取得成功) |
| **bbox 覆蓋率** | **77.77%** | **55.48%** | 100%(影像本質) |
| 分類數 | 72 | 45 | N/A |
| polygon 中位面積 | 2,568 m² | **338 m²** | N/A |
| 真正需要預設色的地表 | 4.75% | 9.73% | 0% |
| 執行期外部依賴 | **無**(離線編譯) | **無**(離線編譯) | **每一格畫面都要** |
| 更新節奏 | 每 1 年 | 持續(活資料) | 服務端決定 |
| 語意 | 土地**被規劃成什麼** | 地表**實際是什麼** | 地表**看起來是什麼** |

**A ∪ B 混合:覆蓋 83.81%,真正需要預設色的地表降到 0.96%。**

**最重要的一個數字**:B 沒蓋到的 5,454,024 m² 裡,**59.2% 是道路廊帶**(FTP-33 會用真的路面 mesh 蓋掉)、**19.0% 在建物腳印底下**(NLSC 3D Tiles 已經蓋住),**真正會露出灰色橢球的只有 9.73%**。詳見 **§7**。

## 2. 量測方法

### 2.1 共用同一套幾何程式

A 與 B **走完全相同的 union / clip / hole 程式碼**([`ground-probe.py`](ground-probe.py) 的 `coverage`、`holes`、`class_areas`、`edge_band`)。若兩邊各用各的幾何邏輯,比較出來的會是兩支 script 的差異而不是兩份資料的差異。

量測 CRS 一律 **EPSG:3826**(公尺),bbox 由 `contracts/constants/m1_area.json` 讀入,**不另存一份**(與 `etl/osm/bbox.py::from_area_file` 同一個來源)。

### 2.2 兩種獨立方法的對帳

headline 覆蓋率同時用**精確聯集**(shapely `unary_union` → `intersection` → `area`)與**網格取樣**(5 m 間距、cell 中心、490,000 個取樣點)量,兩者失敗方式不同,所以一致才有證據力。

| 資料源 | 精確聯集 | 網格取樣(5 m,490,000 點) | 差 |
|---|---|---|---|
| B(OSM) | 0.554774 | 0.554743 | **−3.1 × 10⁻⁵** |
| A(細計圖) | 0.777727 | 0.777712 | **−1.4 × 10⁻⁵** |

再加一層:**同一份 OSM 輸入在兩個不同的 shapely 版本上跑出逐位元相同的結果**(venv shapely 2.1.2 與 anaconda shapely 2.1.1,皆為 `covered_m2 = 6795975.6`、`fraction = 0.554774`、9 個洞且面積清單完全相同)。這排除「數字是某個函式庫版本的產物」。

### 2.3 零值對照組

**本報告出現的每一個 0,都附一個用同一支函式、同一條路徑會回報非零的對照組。** 沒有對照組時,「0 個洞」跟「查詢寫錯了」長得一模一樣。

| 回報 0 的量測 | 對照組(必須非零) | 結果 |
|---|---|---|
| A 的 `holes` 在未排除範圍框時 = 0 | 排除範圍框後同一函式 = 2 個洞、2,722,819 m² | 對照組非零 ✓ |
| `skipped = 0`(兩個資料源皆然) | 考卷 `test_degenerate_rings_are_skipped_and_counted`:3 點環 → `skipped = 1` | 對照組非零 ✓ |
| 完全在 bbox 外的 polygon 貢獻 0 | 同一函式對跨界 polygon 回報 2,500 m²(考卷 `test_polygon_crossing_the_edge_counts_only_the_inside`) | 對照組非零 ✓ |
| 邊緣帶覆蓋 0(考卷合成案例) | 同一函式對滿覆蓋輸入回報 1.0 | 對照組非零 ✓ |
| **data.taipei `/api/v1/dataset/search` 回 `count: 0`** | **空查詢、`'a'`、`'公園'`、`'臺北'`、`'交通'`、`'土地'` 全部也回 0** | **對照組也是 0 → 這個端點無條件回 0,不能用它推論任何事**(見 §4.1) |

最後一列是這條紀律當場救到的一次:若沒有對照組,我會寫成「data.taipei 上找不到土地使用分區資料集」——**那是錯的**,資料集存在(§4.2)。

### 2.4 邊界(bbox 邊緣)有沒有空白帶?

**沒有。** 跨界 polygon 被裁切後,內側部分照算(考卷釘住)。但「有沒有算到」不等於「邊緣看起來有沒有變薄」,所以直接量**沿邊 50 m 帶**的覆蓋率與**內部**的覆蓋率:

| 資料源 | 邊緣帶(50 m)覆蓋 | 內部覆蓋 | 差 |
|---|---|---|---|
| B(OSM) | 58.48% | 55.30% | **+3.18 pp**(邊緣比內部更滿) |
| A(細計圖) | 77.63% | 77.78% | **−0.15 pp** |

兩者都沒有邊緣空白帶。B 的邊緣反而更滿,**成因未判定**——bbox 南緣外是象山山區,大面積 `natural=wood` 跨界進來是一個合理的解釋,但我沒有做能分辨其他解釋的量測,所以不下判定。

跨界元素計數:B 有 54 個 polygon 被裁切、62 個完全在外;A 有 123 個被裁切、13,803 個完全在外(A 是全市檔案,所以「在外」的很多)。

### 2.5 不回述上游 body 是被考卷釘住的

probe 的可用性取樣紀錄**只帶** `at` / `status` / `bytes` / `body_class` / `content_type` / `ms`,錯誤樣本只帶 `at` / `status` / `error_class` / `ms`。考卷用 **allow-list 釘住鍵集合**,不是搜尋某個已知字串:

> 早期版本的考卷只檢查「完整 body 沒有出現在輸出裡」,結果一個把 body 前 16 bytes 以 hex 塞進紀錄的 mutant **活了下來**(M13)。前 16 bytes 也是別人的 body。改成 allow-list 後該 mutant 被殺掉。

**exception 只記型別不記訊息**(`error_class = type(exc).__name__`)——訊息可能夾帶 body 內容。這條也有 mutant 證明過會紅(M6)。

### 2.6 考卷與 mutation

`ground-probe.selftest.py`:**23 案全綠**(合成幾何,不打網路)。

```sh
python docs/spikes/ground-probe.selftest.py
```

三輪 mutation 共 15 個 mutant,**全部殺掉**。其中 4 個是第一輪活下來、逼我補考卷的:

| mutant | 為什麼原考卷抓不到 | 補的案例 |
|---|---|---|
| M9 網格取樣只測 bounding box 不測 containment | 原本所有網格案例都用**軸對齊矩形**,矩形跟它的 bounding box 是同一個集合 | 直角三角形(bbox 邏輯報 1.0,containment 報 0.5) |
| M12 `class_areas` 逐 feature 相加而非同類聯集 | 原案例每類只有一個 feature,相加與聯集相等 | 同類兩個重疊 feature(4,375 vs 5,000) |
| M13 取樣紀錄夾帶 body 前綴 | 原本只搜尋完整 marker | 鍵集合 allow-list |
| M14 `edge_band` 拿邊緣帶跟自己比 | **`edge_band` 當時根本沒有任何案例**(它是宣告後才加的) | 內側/外緣分離 + 零值對照組 |

M14 是誠實的教訓:**declaration commit 之後新增的量測函式,考卷沒跟上。** 本報告倚賴 §2.4 的結論,而那個結論當時是沒有考卷保護的。已補。

## 3. M1 bbox 基準

```sh
cat contracts/constants/m1_area.json
```

EPSG:3826,`e_min` 305500、`e_max` 309000、`n_min` 2767500、`n_max` 2771000 → 3,500 × 3,500 m = **12,250,000 m²**。

Overpass 查詢用的 WGS84 外接框(四角投影後取極值 + 0.0005° 邊界,由 probe 計算,不是手抄):

```text
south 25.013902, west 121.549391, north 25.046632, east 121.585217
```

## 4. 候選 A:data.taipei 都市計畫使用分區

### 4.1 三個發布形態,兩個不能用,一個可以

**「土地使用分區」在 data.taipei 上有三個不同的東西,只有第三個能畫。** 這一節之所以重要,是因為前兩個很容易被當成「A 不可行」的結論——那是錯的。

發現通道本身先踩了一個坑:

```sh
curl -s 'https://data.taipei/api/v1/dataset/search?q=土地使用分區'
# → 200,application/json,67 bytes;結果筆數欄位為 0,結果陣列為空
```

這個端點**對任何查詢(含空查詢)都回 `count: 0`**(§2.3 的對照組)。**它證明不了資料集不存在。** 可用的通道是 SPA 自己打的那支,而且搜尋鍵是 `qs` 不是 `q`:

```sh
curl -s -X POST -H 'Content-Type: application/json' \
  -d '{"qs":"土地使用分區","size":60}' \
  https://data.taipei/api/frontstage/tpeod/dataset.search
```

`qs` 帶查詢 → 202 筆;不帶 → 21,513 筆(全目錄)。**兩個數字不同**,證明過濾真的有作用(這是「搜尋有效」的對照組)。

| 形態 | dataset | 實際內容 | 能不能進 tile |
|---|---|---|---|
| ①「臺北市土地使用分區圖資」 | `a4da6624` | 2,182 bytes CSV,內容是**每個行政區一個 PDF 連結**;信義區 PDF 實際下載 **1,403,294 bytes**,magic `%PDF-` | **不能**——PDF 地圖,需人工套合與數化 |
| ②「臺北市土地使用分區」 | `a132a433` | **36,273,239 bytes CSV**,421,880 列,欄位 `項次,區段代碼,行政區代碼,行政區,大段,小段,母號,子號,分區說明`;1,703 種分區說明;信義區 27,832 筆 | **不能**——**沒有任何座標欄位**,要畫必須另外取得地籍圖幾何 |
| ③「臺北市都市計畫使用分區圖」 | `3bab0a01` | **SHP**,主計圖 2,008,868 bytes + 細計圖 4,187,541 bytes | **可以** |

②「沒有座標欄位」是程式判定的,不是目視:

```sh
python -c "import csv;print([c for c in csv.DictReader(open('zoning-parcels.csv',encoding='utf-8-sig')).fieldnames if any(k in c for k in ['經','緯','X','Y','座標','WKT','TWD'])])"
# → []
```

### 4.2 ③ 的來源事實(可用的那一個)

[dataset 3bab0a01](https://data.taipei/dataset/detail?id=3bab0a01-7936-4218-8cb5-f74dfcb43dda),取得日 **2026-08-11**:

| 欄位 | 值 |
|---|---|
| 格式 | ESRI Shapefile(zip;`.shp`/`.shx`/`.dbf`) |
| 檔案大小 | 主要計畫圖 2,008,868 bytes;細部計畫圖 4,187,541 bytes |
| **CRS** | **檔內無 `.prj`** → 由座標範圍推定為 **EPSG:3826**(見下) |
| 授權 | 依政府資料開放授權條款-第 1 版([OGDL v1](https://data.gov.tw/license)),免費 |
| 發布 / 最後異動 | 2022-07-06 / **2026-04-09** |
| 更新頻率 | 每 1 年(dataset 描述稱資料「更新至 114 年 1 月」) |
| 欄位 | `編號,圖層,顏色,街廓編號,分區代碼,分區簡稱,使用分區,分區說明,原屬分區,變更前代碼,變更前簡稱,變更前分區` |

**CRS 是推定的,我把它標成推定。** 兩個檔的座標範圍都是 E 294,769.3–319,676.1、N 2,759,511.6–2,790,593.6,這與 TWD97 / EPSG:3826 下的臺北市一致(M1 bbox 為 E 305,500–309,000、N 2,767,500–2,771,000,落在其中)。**但檔案本身沒有宣告 CRS**,所以這是我的推定,不是資料的宣告。下游若採用 A,**必須向資料提供者確認 CRS**,不能沿用本報告的推定。

值得注意:屬性表帶一個 **`顏色`** 欄位——資料自己就帶著渲染提示。本 spike 沒有解讀它的值域(§9)。

### 4.3 量測結果(細部計畫圖)

```sh
python docs/spikes/ground-probe.py measure \
  --area contracts/constants/m1_area.json \
  --input <解壓目錄>/細計-面 --source shp --class-field 分區簡稱 \
  --min-hole 100 --grid-step 5 --highways <osm-highways.json> \
  --exclude-class 細部計畫範圍 --exclude-class 規劃範圍
```

**`--exclude-class` 是必要的,而且必須寫在指令裡讓人看見。** 原始檔含兩個**計畫範圍框** polygon(`細部計畫範圍`、`規劃範圍`),各自單獨就蓋滿整個 bbox(各 12,250,000 m²)。不排除的話覆蓋率是 **100.0%**,而那個 100% 描述的是「這裡屬於某個都市計畫」,不是「這塊地是什麼」。probe 把排除清單原樣印進輸出,所以覆蓋率不可能偷偷依賴一個看不見的過濾器。

| 指標 | 值 |
|---|---|
| **覆蓋率** | **77.7727%**(9,527,151.8 m²) |
| 網格對帳(5 m) | 77.7712%(差 −1.4 × 10⁻⁵) |
| 分類數 | **72** |
| bbox 內 polygon 數 | 1,738 |
| 洞(≥100 m²) | 2 個,合計 2,722,819 m² |

面積最大的 12 類(m²):

| 分區簡稱 | 面積 | | 分區簡稱 | 面積 |
|---|---|---|---|---|
| 保 | 2,531,644 | | 住 | 214,357 |
| 住3 | 2,097,599 | | 機 | 205,408 |
| 公 | 720,421 | | 特 | 180,645 |
| 住4 | 481,237 | | 住3-1 | 177,999 |
| 商三特 | 378,433 | | 業務 | 177,934 |
| 特業 | 222,557 | | 國小 | 171,799 |

### 4.4 粒度:街廓內部到底有沒有細分?

**這是決定 A 能不能用的那一題,答案分兩半:**

| 圖層 | bbox 內 polygon 數 | p25 | **中位數** | p75 | p95 | 分類數 | 覆蓋率 |
|---|---|---|---|---|---|---|---|
| **主要計畫圖** | 271 | 2,305 m² | **8,809 m²** | 27,193 m² | 130,574 m² | 34 | 82.47% |
| **細部計畫圖** | 1,738 | 1,207 m² | **2,568 m²** | 4,299 m² | 10,512 m² | 72 | 77.77% |
| (對照)**OSM** | 1,674 | 73 m² | **338 m²** | 1,794 m² | 11,713 m² | 45 | 55.48% |

**主要計畫圖:owner 的擔心完全正確。** 中位 8,809 m² —— 臺北典型街廓約 100 × 100 m ≈ 10,000 m²,所以主計圖**確實是一個街廓一個顏色**,p75 到 27,193 m² 更是好幾個街廓連成一片。**主計圖對「開車時看到的地面」太粗,不建議採用。**

**細部計畫圖:有細分。** 中位 2,568 m² ≈ **四分之一個街廓**,polygon 數是主計圖的 6.4 倍,分類數是 2.1 倍。**街廓內部確實被切開了。**

**但仍比 OSM 粗 7.6 倍**(中位 2,568 vs 338 m²)。

### 4.5 河岸、公園、空地分得出來嗎?

**公園:分得出來。** `公`(公園用地)720,421 m²。與 OSM 的 `leisure=park` 773,397 m² 為**同一數量級的獨立佐證**——兩份完全獨立的資料對同一片地給出相近面積。

**山坡/綠地:分得出來。** `保`(保護區)2,531,644 m²,對應 OSM `natural=wood` 2,761,119 m²(象山一帶)。同樣互相佐證。

**河岸:M1 bbox 內答不了。** A 在 bbox 內的水相關分類只有 `自來水廠` 19,698 m²、`排水溝` 15,400 m²、`調節池` 941 m²;OSM 的 `natural=water` 也只有 26,483 m²。**信義 bbox 內本來就幾乎沒有水域**(全市層級的地號 CSV 有 `河川區` 3,638 筆,但不在本 bbox)。→ **「河岸表現如何」這一題不能用 M1 的資料回答,必須等擴張到有河的範圍再測。** 我沒有量到,所以不下結論。

**空地:兩邊都不可靠。** A 給的是**規劃用途**(某塊地規劃為 `住3` 不代表現在有房子);OSM 有 `landuse=construction` 117,788 m²、`landuse=brownfield` 33,562 m²,但那取決於有沒有人去標。**「空地」在兩個資料源裡都不是一個可信的類別。**

### 4.6 幾何品質與 ingestion 風險

| 指標 | 細計圖 | 主計圖 |
|---|---|---|
| shape 數 | 15,518 | 3,840 |
| 環總數 | 15,569 | 4,080 |
| 多環 shape | **26** | **119** |
| 無效幾何(自交等,已修復並計數) | 122 | 34 |
| 完全無法構成面而跳過 | 0 | 0 |
| **沒有任何順時針環的 shape** | **15,394 / 15,518** | **3,685 / 3,840** |

最後一列是一個**實測到的規格偏離**:ESRI shapefile 規範以「外環順時針、內環逆時針」表達洞,而這份檔案 **99.2% 的 shape 沒有任何順時針環**。換句話說,**這份檔案的環方向不帶洞的資訊**。

影響有限但必須揭露:多環 shape 只有 26 個(細計)/ 119 個(主計),其餘皆為單環,單環時方向不影響面積。**但若下游要正確處理那 26 個帶洞的 shape,不能依賴環方向,要另外用幾何包含關係判定。** probe 現在的作法是:當一個 shape 完全沒有順時針環時,把所有環當外環並計數(所以上表那個數字才存在)。

**A 的 ingestion 風險清單**:無 `.prj`(CRS 需確認)、環方向不符規範、屬性為 Big5/CP950 編碼(需指定 `--encoding cp950`)、檔內檔名亦為 CP950。

## 5. 候選 B:OSM landuse / natural / leisure

### 5.1 取得方式:**沒有既存的 extract,我改用 bbox 範圍查詢**

**票上與派工說明都寫「我們已經在下載並保存臺北 OSM extract 了,先用現有的 extract 統計」。這個前提不成立。**

FTP-29 交付的是**取得與裁切的程式**,不是**一份保存下來的 extract**:

```sh
git ls-tree -r --name-only origin/develop | grep '^scene-pipeline/src/scene_pipeline/etl/osm/'   # 模組在
find /c/furious-tp /c/.yforce-worktrees-furious-tp -iname '*.pbf' -o -iname '*.osm*'             # 只有測試 fixture
find /c/Users/chihl -maxdepth 4 -iname '*.osm.pbf'                                              # 空
```

全機器上唯一的 OSM 檔是各 worktree 內的 `scene-pipeline/tests/etl/osm/fixtures/xinyi_highways.osm`(**只含 highway,沒有任何 landuse**),沒有任何 `.pbf`。`scene-pipeline/.gitignore` 亦確認 ETL 產物不進 repo。

**所以我沒有「用現有 extract」,也沒有去抓 325 MB。** 改用 Overpass 只取 M1 bbox 範圍——底層是同一份 OSM 資料,量級差兩個數量級:

| 取得內容 | bytes |
|---|---|
| landuse/natural/leisure/waterway/amenity/place(面) | 2,939,943 |
| highway(算路廊用) | 3,836,597 |
| building(算建物遮蔽用) | 7,265,204 |
| **合計** | **14,041,744**(≈ 13.4 MB,對比 Geofabrik 全臺 extract 約 325 MB) |

```sh
python docs/spikes/ground-probe.py osm \
  --area contracts/constants/m1_area.json --out <輸出目錄>
```

**取得過程本身有兩件工程事實值得寫進來:**

1. **`overpass-api.de` 對 `python-requests` 的預設 User-Agent 回 406。** 同一條合法查詢、同一秒、只差 header:預設 UA → **406**;帶自訂 UA → **200**。**若沒發現這件事,本報告會把一個活著的來源寫成死的。** probe 現在固定帶 UA,並把理由寫在常數旁邊。
2. **Overpass 會限流。** 首次嘗試兩個 mirror 皆 **429**;成功那次是 `overpass-api.de` 回 504、改由 `overpass.kumi.systems` 於 196 秒回 200。probe 因此**記錄每一次嘗試**而不只記最後一次。→ **Overpass 適合做 spike,不適合做正式 pipeline 的取得通道**;正式採用 B 時應改用 Geofabrik extract(FTP-29 既有路徑)。

### 5.2 量測結果

```sh
python docs/spikes/ground-probe.py measure \
  --area contracts/constants/m1_area.json \
  --input <輸出目錄>/osm-areas.json --source osm \
  --min-hole 100 --grid-step 5 --highways <輸出目錄>/osm-highways.json
```

| 指標 | 值 |
|---|---|
| Overpass 回傳元素 | 2,290 |
| 採用為地表面的 feature | **1,736**(way 1,567 + relation 169) |
| 濾掉:非地表的 `amenity`/`place`/`waterway` 值 | 470 |
| 濾掉:未閉合的 way | 131 |
| relation 無法組成環 | 2 |
| **覆蓋率** | **55.4774%**(6,795,975.6 m²) |
| 網格對帳(5 m) | 55.4743%(差 −3.1 × 10⁻⁵) |
| 分類數 | **45** |
| 洞(≥100 m²) | 9 個,合計 5,453,718 m²(最大一個 5,427,397 m²,即幾乎全部連成一塊) |

面積最大的 12 類(m²):

| 類別 | 面積 | | 類別 | 面積 |
|---|---|---|---|---|
| `natural=wood` | 2,761,119 | | `landuse=industrial` | 148,916 |
| `landuse=residential` | 1,311,669 | | `landuse=retail` | 133,467 |
| `leisure=park` | 773,397 | | `landuse=construction` | 117,788 |
| `amenity=school` | 434,824 | | `landuse=recreation_ground` | 82,928 |
| `landuse=commercial` | 419,440 | | `amenity=hospital` | 76,714 |
| `landuse=grass` | 201,177 | | `landuse=military` | 72,124 |
| `landuse=cemetery` | 199,893 | | `amenity=university` | 51,084 |

**分類語意直接對應外觀**(`wood`、`park`、`grass`、`water`、`pitch`),這是 B 相對 A 的核心優勢:A 的 `住3`、`商三特` 要先經過一層「規劃類別 → 長什麼樣」的假設才能上色。

**注意各類面積相加(7,279,174 m²)大於聯集(6,795,976 m²),差 483,198 m²**:類別之間有重疊(例如公園界線內又標了草地)。probe 對**同類**做聯集(所以單一類別不會自己灌水),但**不跨類去重**,因為那需要一套優先順序規則,而那是 tile 格式票要決定的事,不是量測。

### 5.3 幾何品質

| 指標 | 值 |
|---|---|
| 無效幾何(自交等,已修復並計數) | **8** |
| 完全無法構成面而跳過 | **0**(對照組見 §2.3) |
| 未閉合而排除的 way | 131 |
| relation 有 outer 環但組不起來 | 2 |
| 跨 bbox 邊界被裁切 | 54 |

**幾何品質比 A 好一個量級**(8 個無效 vs 122 個),且閉合性問題(131 個未閉合 way)大多是本來就不該是面的線狀物。

### 5.4 能不能共用 FTP-29 的裁切器?**不能**(附數字)

**票上問的是「能不能共用同一套裁切器」,答案是不能,而且理由用面積才看得出來:**

| | feature 數 | 佔元素數 | **各自聯集面積(m²)** | **佔總覆蓋(6,795,976 m²)** |
|---|---|---|---|---|
| way | 1,567 | 90.3% | 2,914,638 | 42.9% |
| **relation** | **169** | **9.7%** | **4,109,932** | **60.5%** |

**relation 只佔元素數的 9.7%,卻承載 4,109,932 m² —— 總覆蓋面積的 60.5%。**

(兩列相加 7,024,570 m² 大於聯集 6,795,976 m²,差 228,594 m²,因為 way 與 relation 的幾何彼此重疊;故上表最後一欄不是一個分割,兩者相加會超過 100%。若改以「way 聯集 + relation 聯集」為分母,relation 佔 **58.51%**。兩種算法我都列出來,因為選哪一個分母會改變數字,而結論在兩種算法下相同。)

而 `scene-pipeline/src/scene_pipeline/etl/osm/read.py` 只走 `osmium.osm.WAY`:

```python
for way in osmium.FileProcessor(str(path), osmium.osm.WAY):
```

→ **直接沿用 `read_extract` 會漏掉六成的地表面積。** 大面積的 landuse(象山林地、河濱、大型公園)在 OSM 裡通常是 multipolygon relation。

第二個不能共用的地方:`etl/osm/bbox.py` 的 `clip_polyline` 是**折線**裁切(Liang-Barsky + 端點吸附),回傳的是 `ClippedRun` 序列。**面的裁切不是折線裁切**——面被 bbox 切開後要重新閉合成環,折線裁切不做這件事。

**可以共用的是 `BBox` 本身**(`from_area_file`、`contains`、min-inclusive/max-exclusive 的邊界歸屬規則),那是契約層的東西,應該共用以免兩處對邊界歸屬的定義漂移。

→ **下游 ETL 票必須寫明:需要新增 relation(multipolygon)讀取與面裁切,不能宣稱沿用 FTP-29。** 這是本 spike 對下游最具體的一條輸入。

## 6. 候選 C:正射影像(NLSC WMTS)

### 6.1 端點與金鑰

```sh
curl -sI 'https://wmts.nlsc.gov.tw/wmts/PHOTO2/default/GoogleMapsCompatible/18/112233/219592'
```

| 項目 | 實測結果(2026-08-11) |
|---|---|
| 端點 | `https://wmts.nlsc.gov.tw/wmts/{layer}/default/GoogleMapsCompatible/{z}/{y}/{x}` |
| 實測可用圖層 | `PHOTO2`、`PHOTO_MIX`、`EMAP`(皆 200 / `image/jpeg`) |
| Capabilities | `https://wmts.nlsc.gov.tw/wmts/1.0.0/WMTSCapabilities.xml`(200,`text/xml`,182,301 bytes) |
| **是否需要金鑰** | **否** —— 48 次請求全部未帶任何憑證,48 次都成功 |
| 單磚大小 | 19,816–26,315 bytes(z=18,24 塊相異磚實測) |

### 6.2 可用性取樣:48 次全綠 —— 以及這個數字證明不了什麼

**兩輪,共 48 次取樣,全部成功。**

| 輪次 | 對象 | n | 200 | 錯誤 | body 類別 | p50 | p95 | 時間窗(UTC) |
|---|---|---|---|---|---|---|---|---|
| 1 | **同一塊磚**重複取 | 24 | 24 | 0 | jpeg × 24 | 31 ms | 47 ms | 14:24:25 → 14:26:21 |
| 2 | **24 塊相異磚** | 24 | 24 | 0 | jpeg × 24 | 32 ms | 94 ms | 14:51:42 → 14:52:29 |

第 1 輪的 24 個回應**位元組數完全相同(23,030)**,所以它很可能量的是同一個快取項而不是來源服務。**第 2 輪因此改取 24 塊相異磚,得到 24 個相異位元組數(19,816–26,315)**,排除「整輪都打在同一個快取上」。

**這 48 次全綠證明不了服務可靠。** 兩個時間窗各約 1–2 分鐘。**FTP-5 於同一主機家族實測到的故障型態是「16/17 端點連續約 40 分鐘回 100% NUL bytes」**——那種故障用 2 分鐘的窗**必然量不到**。

→ **我的量測沒有反駁 C 的可靠性,也沒有證實它。** 不採用 C 的理由(§8.3)建立在授權與架構上,**不建立在我量到的可用性上**。這一點我寫明,避免報告用一個不成立的理由支撐一個我認為正確的結論。

(probe 已內建把「HTTP 200 但 body 全為 NUL」歸為 `all_nul` 類且**不計入 ok**,並有 mutant 證明過該分支會紅。本次 48 次取樣中此類別計數為 **0**——對照組是考卷裡的 `test_sampler_counts_nul_bodies_as_a_class_not_a_success`,同一支函式對全 NUL 輸入回報 `all_nul` 且 `ok = 0`。)

### 6.3 離線快取進 tile:授權未裁定

**若能離線快取進 tile,執行期依賴就消失,C 的取捨會整個改變。所以這一題是 C 的關鍵。**

**引用既有裁定,不重新論證:**

- [FTP-5 報告 §1.3](nlsc.md)(轉載/快取限制):服務使用條款**沒有任何條文**規範使用者端快取、代理、鏡像或再散布,「**既無禁止,也無許可**」→ 結果標記 **`unverified`**。並記錄第三點「本服務將會不斷更新」對長期快取構成規範上的張力。
- [`LICENSING.md`](../../LICENSING.md)(FTP-6):明文**不對 NLSC 圖資授權作結論**,並記載既有的 data.gov.tw dataset 不足以證明 NLSC 圖資之授權依據。

→ **把 NLSC 正射影像烘進我們自己散布的 tile,目前沒有已確立的授權依據。** 這不是「禁止」,是**未裁定**;要走這條路必須先讓 human 去取得授權確認,而那是 FTP-5 已經標成 `unverified` 而未再前進的事項。

## 7. 「沒被蓋到的」到底是什麼 —— 本報告最關鍵的一節

**單看覆蓋率會嚴重高估問題。** bbox 內沒被 landuse polygon 蓋到的地方,不代表會露出灰色橢球——**道路會被 FTP-33 的路面 mesh 蓋掉,建物腳印會被已經在畫面上的 NLSC 3D Tiles 蓋掉。**

所以把未覆蓋區域拆成三塊:**道路廊帶**(highway 中心線 8 m 緩衝)、**建物腳印**(OSM building,9,362 個,聯集 2,665,075 m² = bbox 的 21.76%)、**其餘**。

### 7.1 拆解結果

| 資料源 | 覆蓋率 | 未覆蓋合計 | 其中道路廊帶 | 其中建物腳印 | **真正的空白** | **佔 bbox** |
|---|---|---|---|---|---|---|
| **B**(OSM) | 55.48% | 5,454,024 m² | 3,228,360 | 1,033,747 | **1,191,918 m²** | **9.73%** |
| **A**(細計圖) | 77.77% | 2,722,848 m² | 2,125,489 | 15,750 | **581,609 m²** | **4.75%** |
| **A ∪ B** | **83.81%** | 1,983,292 m² | 1,861,703 | 4,394 | **117,196 m²** | **0.96%** |

指令:

```sh
python docs/spikes/ground-probe.py combine \
  --area contracts/constants/m1_area.json \
  --osm <osm-areas.json> --shp <解壓目錄>/細計-面 --class-field 分區簡稱 \
  --exclude-class 細部計畫範圍 --exclude-class 規劃範圍 \
  --highways <osm-highways.json>
```

(建物拆解那一欄由同一支 probe 的函式計算,額外載入 `building` 查詢結果;查詢見 §5.1。)

### 7.2 這改變了什麼

**B 單獨的真實缺口是 9.73%,不是 44.5%。** 「OSM 覆蓋率只有五成半」聽起來像不能用,但其中 **59.2% 是道路、19.0% 是建物底下**,兩者都已經有東西蓋。**B 單獨可用**,只是有約一成的地表要吃預設色。

### 7.3 混合方案 A+B 的實測

**要不要用 A 補 B 的洞?——要,而且邊際效益具體:**

- A 填掉了 B 的洞的 **63.64%**(3,470,732 m² / 5,454,024 m²)
- 真正的空白從 **9.73% → 0.96%**,即**多救回 8.77 個百分點的地表**(約 1.07 km²)
- 合併後剩下的 1,983,292 m² 裡有 **93.87% 是道路廊帶**——那是 FTP-33 的工作,不是著色的工作

**8.77 pp 是不是值得多接一個資料源,是 owner 的取捨,不是我的。** 我給的是這個數字和它的代價(§8.1)。

## 8. 建議:主案 + 備案 + 選它會放棄什麼

**這一題不由 agent 定案。以下是建議,裁決在 owner / RFC。**

> **關於 owner 對 B 的初步偏好:量測支持它,但不是無條件支持。**
> B 的**分類語意**確實比 A 適合著色(`wood`/`park`/`grass` 直接對應外觀;`住3`/`商三特` 要多一層假設),粒度也細 7.6 倍,幾何品質好一個量級,而且零新資料源。
> **但 B 單獨會留下 9.73% 的地表沒有分類**,而 A 能把它壓到 0.96%。所以我的建議是「**以 B 為主**」而不是「**只用 B**」。

### 主案:**B 為主分類 + A 補洞(皆離線編譯進 tile)**

1. 地表分類以 **OSM landuse/natural/leisure** 為準(粒度細、語意直接對應外觀)。
2. OSM 未覆蓋處,以 **data.taipei 都市計畫「細部計畫圖」** 補(**不要用主要計畫圖**,中位 8,809 m² 一個街廓一個顏色,太粗)。
3. 剩下的 0.96% 吃一個中性預設色;道路廊帶交給 FTP-33 的路面 mesh。
4. **零執行期外部依賴、純開放資料。**

### 備案:**B 單獨**

同上但不接 A,9.73% 的地表吃預設色。**適用情境**:若 owner 判斷「多接一個每年更新、CRS 未宣告、環方向不符規範的來源」不值得那 8.77 pp。

### 8.1 選主案(B + A 混合)放棄了什麼

1. **授權變複雜,而且踩到一個 FTP-6 明文未裁定的前提。** 依 [`LICENSING.md`](../../LICENSING.md),把 OSM 資料併進某圖層會使該圖層成為 **ODbL Derivative Database**,share-alike 外溢(FTP-6 對 props/physics 層明文下過這條禁令)。合併後的地表層若視為單一資料庫,**整層落入 ODbL**,連帶把 OGDL v1 來源的內容一起帶進 share-alike 範圍;若想讓兩層各自保有授權,需要依賴 FTP-6 標為「**前提未裁定**」的 Collective Database 定位。**這是本主案最實質的代價,而且它是一個 human 決定,不是工程決定。**
2. **兩種更新節奏**:OSM 持續變動、A 每 1 年。兩者不同步時,補洞的邊界會跟著 OSM 漂移。
3. **A 的 ingestion 風險**:無 `.prj`(CRS 需向提供者確認,本報告只做推定)、99.2% 的 shape 環方向不符 ESRI 規範、CP950 編碼。
4. **語意混用**:同一張畫面上,一部分顏色代表「地表實際是什麼」,另一部分代表「這塊地被規劃成什麼」。**規劃用途可能與現況不符**(規劃為 `住3` 的地可能是工地),混合後**看起來會很有把握地畫錯**。這是我認為最容易被低估的一項。
5. 多一份 4 MB 來源要版本控管與標示。

### 8.2 選備案(B 單獨)放棄了什麼

1. **9.73% 的地表(約 1.19 km²)沒有分類**,只能吃預設色。以 M1 的尺度,那是駕駛會看到的一整片。
2. 放棄 A 對 B 的**交叉佐證**(§4.5 的 `公` vs `park`、`保` vs `wood`)。
3. **OSM 是活資料**:覆蓋率會隨編輯漂移,可能變好也可能變差,而沒有第二個來源兜底。

### 8.3 不採用 C 放棄了什麼

**先講清楚不採用的理由不是什麼:不是我量到它不可靠(48/48 全綠)。** 理由是:

1. **授權未裁定**(§6.3):要把影像烘進我們散布的 tile,目前沒有已確立的授權依據。而**不烘進 tile 就等於接受執行期外部依賴**。
2. **執行期外部依賴的後果不對稱**:FTP-5 實測過同主機家族「16/17 端點連續約 40 分鐘壞掉」。**建物壞掉是少了建物;底圖壞掉是整個地表變空白。**
3. 與 PRD「公開免登入、純臺灣開放資料」相容,但與 A/B 的「零執行期依賴」不相容。

**放棄的是**:照片級的真實感,以及「不需要任何分類規則就能讓地表看起來對」的省事。**若 owner 願意由 human 去取得 NLSC 的快取/再散布授權確認,C 的取捨會整個翻轉**(離線烘焙後它就沒有執行期依賴,而真實感是 A/B 給不了的)。**那是一個 human 決定,我不推進。**

### 8.4 風格取捨(這一段是判斷,不是量測)

**建物本來就是無貼圖的實體塊**(NLSC 3D Tiles),路面是 mesh,路樹路燈是 instance。**在這個場景裡,依用途著色的地表是風格一致的;照片級底圖反而會讓無貼圖的建物顯得突兀。** 這是設計判斷,不是我量出來的,列在這裡是因為票要求回答,**它不應該被當成證據使用**。

## 9. 未量測與做不到的事

**不用文件描述代替量測;以下每一項都是我沒做到的。**

1. **我沒有用 Geofabrik 全臺 extract。** B 的數字來自 Overpass 的 bbox 查詢。兩者底層同源,但**我沒有實測兩者對同一 bbox 是否給出相同結果**。正式採用 B 前應以 extract 路徑複核一次。
2. **A 的 CRS 是推定的**(座標範圍),不是資料宣告的。檔內無 `.prj`。
3. **A 的 `顏色` 欄位我沒有解讀。** 它可能直接給出官方配色,對下游有用。
4. **河岸表現無法從 M1 判斷**(§4.5):bbox 內幾乎沒有水域。
5. **C 的可用性只涵蓋兩個各約 1–2 分鐘的窗**(§6.2),**量不到 FTP-5 觀察到的 40 分鐘故障型態**。
6. **我沒有渲染任何東西。** 本報告沒有一張圖是我看過的畫面;「看起來像什麼」全部是推理,不是觀察。
7. **我沒有量編譯進 tile 的成本**(檔案大小、產出時間)。那取決於尚未決定的 tile 格式。
8. **B 的類別重疊我沒有解決**,只揭露(§5.2)。跨類優先順序是 contracts 票要定的。
9. **`amenity`/`place` 的地表化白名單是我定的**(`parking`/`school`/`university`/`hospital`/`college`/`kindergarten`/`square`),**未經第三方確認**;換一組白名單覆蓋率會變。這是本報告覆蓋率數字裡最主觀的一個選擇。
10. **邊緣帶為何比內部更滿,成因未判定**(§2.4)。

## 10. 授權(引用既有裁定,不重新論證)

### 10.1 B(OSM)— ODbL 1.0

依 [`LICENSING.md`](../../LICENSING.md)(FTP-6,已裁定):衍生 tiles **可公開散布**;OSM 衍生資料庫為 **ODbL Derivative Database**,義務為 share-alike、attribution(`© OpenStreetMap contributors`)、隨資料庫本體交付授權連結(**Databases safe harbour**,僅 HUD 標示不足)、免費提供機器可讀之全量衍生資料庫。FTP-29 已建立來源隔離目錄與 `ATTRIBUTION.md` 機制。

### 10.2 A(data.taipei)— OGDL v1

依同文件:data.taipei 來源適用[政府資料開放授權條款第 1 版](https://data.gov.tw/license),**顯名標示為失權要件**(未盡標示義務視為自始未取得授權)。改作、散布、再轉授權均在授權範圍內。

**本 spike 新增的一項提醒**(不是新裁定,是把既有裁定套到本題):FTP-6 對 props/physics 層下過「**不得引入 OSM 來源資料**」的工程約束,理由正是 share-alike 外溢。**地表著色層若採主案(A+B 合併),就是刻意讓一個含 OGDL 來源的圖層變成 ODbL Derivative Database。** 這在 road tiles 上已經是既定作法(FTP-6 表格第一列),但**地表層要不要比照,必須由 human 明確裁定**,不能由本 spike 推定。

### 10.3 C(NLSC)— `unverified`

FTP-6 明文**不對 NLSC 圖資授權作結論**;FTP-5 §1.3 認定服務條款**未規範**快取/代理/再散布,標記 `unverified`。→ **離線烘焙進散布物的授權依據目前不存在**(§6.3)。

## 11. 外部參照:hop.earth / three-geo(**二手,推論邊界寫在前面**)

**這一節不是資料,是一個參照。它沒有影響上面任何一個數字。**

**二手部分(我沒有第一手觀察)**:coordinator 轉述,`hop.earth` 這個站在做類似的可駕駛/可行走真實世界 3D 場景,其 `ATTRIBUTIONS.md` 列出 three.js + Bullet(Ammo.js)+ three-geo + Copernicus/IGN/CNIG 高程 + OpenStreetMap 向量 + LERC/Draco/ASTC 壓縮,而**該檔案中未見任何衛星或正射影像的授權標示**。

**推論邊界(必須連著讀)**:

1. **我沒有觀察過 hop.earth 的任何畫面,也沒有讀過那個 `ATTRIBUTIONS.md`。** 該站對 WebFetch/curl 回 403。**這一整段是轉述。**
2. **「授權清單裡沒有」不等於「沒有用」。**
3. **這一點我第一手查證了,而且它削弱上面的解讀**:`three-geo`([w3reality/three-geo](https://github.com/w3reality/three-geo),MIT)的 README 明載其地形建構**以 Mapbox Maps API 的衛星圖磚上貼圖**,且 `tokenMapbox` 「**must be provided**」;另一個模式 `getTerrainVector` 產出的是**等高線圖**(`dem-vec-shade-<ele>-`),**不是 landuse 著色**。
   → **這個函式庫的兩個模式都不是「OSM landuse 多邊形著色」**:一個是需要金鑰的商用衛星影像,一個是高程等高線。
4. 它是 three.js 架構,本專案是 Cesium(RFC D1)。**地表著色的資料源選擇與渲染引擎無關**,所以參照仍有效;但**本報告不對 D1 表示任何意見**(D1 的翻案觸發器是空場景 + NLSC 基礎開銷 > 6 ms,屬 FTP-48/49,尚未量測)。

**結論**:**這個參照不能當成「選項 B 的存在證明」。** 它證明的是「有人用 OSM 向量 + 國家級高程做出可用的場景」,但它所用的函式庫的預設地表著色是**商用衛星影像(需金鑰)**,那正是本專案的 PRD 排除的東西。**我未觀察到的東西,不能當成證據。**

**一個附帶有用的線索**(不在本 spike 的 Scope,交 contracts 票參考):該清單使用 **LERC**(高程柵格)、**Draco**(網格)、**ASTC**(貼圖)壓縮,是已被實務驗證的選擇,可作為 tile 格式票的量級參考。**格式定案不在本 spike。**

## 12. 對下游票的影響(裁決後才開,本 spike 不開)

若 owner 裁定採用主案或備案:

1. **`scene-pipeline/` ETL 票**必須寫明:**需要 relation(multipolygon)讀取與面裁切,不能宣稱沿用 FTP-29 的 `read_extract` / `clip_polyline`**(§5.4,relation 承載總覆蓋面積的 60.5%)。可共用的是 `BBox` 的邊界歸屬規則。
2. **`contracts/` tile 層票**:需決定跨類優先順序(§5.2 的類別重疊)、預設色、以及 A/B 兩來源的圖層關係(單一資料庫 vs Collective Database——**這一項牽動授權,見 §10.2**)。
3. **`web-client/` provider 票**:地表層在 Cesium 中的載入與 terrain(FTP-40)的疊合順序。
4. **若採主案,先由 human 裁定 §10.2 的授權定位**,再開 1–3。

---

**本報告的所有數字由 [`ground-probe.py`](ground-probe.py) 於 2026-08-11 產生,指令附在各節。OSM 與線上服務為活資料,重跑會得到不同的點值。**
